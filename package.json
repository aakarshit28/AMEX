{
  "name": "atlas-full-stack",
  "version": "1.0.0",
  "description": "ATLAS by American Express — Full Stack App",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install && npm run build && cd .. && node -e \"require('fs').cpSync('client/dist', 'dist', {recursive: true})\"",
    "start": "cd server && node src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.7.0",
    "pdfkit": "^0.15.0",
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
